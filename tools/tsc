#!/bin/bash

set -eu

source .env

docker run \
    --rm \
    --interactive \
    --tty \
    --init \
    --user "$(id -u):$(id -g)" \
    --volume "$PWD:/srv/app" \
    --workdir /srv/app \
    ${NODE_IMAGE} \
    ./node_modules/.bin/tsc "$@"
